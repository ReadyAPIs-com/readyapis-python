"""Single source of truth for the SDK version.

Keep this file dependency-free so it can be imported before anything else.
The version is also declared in ``pyproject.toml``; bump both when releasing.
"""

__version__ = "0.1.0"
